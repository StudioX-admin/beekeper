// 파비콘은 이미지 파일이므로 텍스트로 직접 만들 수 없습니다.
// GitHub에 업로드할 때는 실제 .ico 파일을 업로드해야 합니다.
// 간단한 파비콘은 https://favicon.io/ 등의 웹사이트에서 생성할 수 있습니다.
// 또는 임시로 다음 단계를 수행할 수 있습니다:

// 1. 빈 파비콘 파일 생성하기
// GitHub에서 "Add file" → "Create new file" 클릭
// 파일 이름에 "app/public/favicon.ico" 입력
// 내용은 비워두고 "Commit new file" 클릭

// 2. 나중에 실제 파비콘 파일로 교체하기
// "Add file" → "Upload files" 클릭
// 파비콘 파일을 선택하고 "app/public/favicon.ico" 경로에 업로드
